export default class Cupom {
  constructor(id, nome, desconto, dataLimite, valor) {
    this.id = id;
    this.nome = nome;
    this.desconto = desconto;
    this.dataLimite = dataLimite;
    this.valor = valor;
  };
};